-- local colorscheme = "nord"
--
-- local ok, _ = pcall(vim.cmd, "colorscheme " .. colorscheme)
-- if not ok then
--   -- Level 3 == ERROR (:h notify)
-- 	vim.notify("colorscheme '" .. colorscheme .. "' not found", 3)
--   vim.cmd [[colorscheme koehler]]
-- 	return
-- end

